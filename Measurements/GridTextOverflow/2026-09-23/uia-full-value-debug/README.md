# Complete UIA values for shortened multiline cells — x64 Debug

This sibling packet archives the successful x64 Debug follow-up for the focused Grid UIA witness. `build.ps1 -Configuration Debug -Platform x64 -Rebuild` exited 0. `test.ps1 -Configuration Debug -Platform x64 -SkipBuild -Suites Grid,Rendering,Embedded,Accessibility` exited 0; all four suite receipts report zero capability skips. The source HEAD is `ba983fe15d42e0632d7e03aa3db1780a5d1b9511`; `raw/inputs.receipt.txt` and the exact copied witness source identify compiled inputs and the raw suite receipts identify executable hashes.

Raw evidence came from `C:/RedSalamander.Perf/evidence/i26-ui/grid-uia-debug-20260923/`. The exact source driver is separately copied from `C:/RedSalamander.Perf/evidence/i26-ui/run-grid-uia-debug-20260923.ps1` to `raw/`; it has SHA-256 `5a207908e1d88b8965f98b6493df65f49ca5efd610c872f737a54379d99753f5`. Source bytes are copied unchanged, with non-Performance JSON renamed to `.receipt.txt`. `receipt-filenames.md` maps renamed files, and `SHA256SUMS.txt` binds every archived byte.

The automatic complex-UI benchmark comparison is `unpaired`; it makes no performance acceptance claim and does not replace the original matched resource gate. The focused witness's six-profile matrix is x64 Debug, Release and ASan Debug plus native ARM64 Debug, Release and ASan Debug. x64 Debug is this packet. x64 Release is archived separately in `uia-full-value-release`; the two archived witness sources are text-identical after CRLF normalization. The remaining witness profiles are x64 ASan Debug (including its detection probe) and all three ARM64 profiles, each requiring a fresh cross-build and native Grid, Rendering, Embedded and Accessibility execution. None was run by this curation script. The new witness does not by itself qualify native assistive technology, consumer adoption, or the remaining GridTextOverflow plan gates.