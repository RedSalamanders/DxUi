# Raw standalone DxUi evidence

The enclosing packet defines successful, failed, diagnostic and unpaired results. Raw performance receipts are unchanged; their presence does not imply performance acceptance. Other JSON retains its original bytes in `.receipt.txt` files.
