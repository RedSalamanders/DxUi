# V11 ASan-Debug functional qualification

All 16 non-activating library suites pass, zero skips. ASan additionally retains
its deliberate use-after-free detection receipt where applicable. Suite JSON
preserves the emitted outcomes/executable identities and adds library/benchmark
provenance from its linked performance report. `originalReceiptSha256` identifies
the unchanged raw receipt retained at
`C:/RedSalamander.Perf/evidence/i26-ui/dxui-grid-v11-final-ASan-Debug`.
Performance reports are unchanged copies. The ASan text log normalizes trailing whitespace; its original raw log remains in the external qualification folder. These benchmark reports are
unpaired; the parent record's matched comparisons and unresolved resource flags
remain authoritative. Foreground and native ARM64 qualification are separate.
