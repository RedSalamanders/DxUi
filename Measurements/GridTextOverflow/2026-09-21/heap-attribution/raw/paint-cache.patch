diff --git a/src/Controls/DxUi.Grid.cpp b/src/Controls/DxUi.Grid.cpp
index 6707e91..db5bdf8 100644
--- a/src/Controls/DxUi.Grid.cpp
+++ b/src/Controls/DxUi.Grid.cpp
@@ -2049,14 +2049,13 @@ void Grid::Paint(ControlHost& host) const
         entry.usedInPaint = false;
     const auto releaseOffscreenLayouts = wil::scope_exit([&]() noexcept
     {
-        // Reuse bounded string storage, but do not retain shaped COM layouts
-        // for values that have scrolled out of this grid's visible working set.
+        // Experiment: share shaping within one paint, retaining only string
+        // storage across paints to test native heap fragmentation from layouts.
         for (auto& entry : _cellTextLayouts)
-            if (! entry.usedInPaint)
-            {
-                entry.layout.reset();
-                entry.format.reset();
-            }
+        {
+            entry.layout.reset();
+            entry.format.reset();
+        }
     });
     _lastPaintHadAnimatedVisibleCells  = false;
     _animatedVisibleCellStateValid     = true;
