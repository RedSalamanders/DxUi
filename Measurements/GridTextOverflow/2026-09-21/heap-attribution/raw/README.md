# Raw grid diagnostic packet

The [reviewed explanation](../README.md) defines each baseline, candidate,
rejected experiment and comparison limit. Benchmark JSON and comparison files
are unchanged standalone DxUi receipts with matching fixture fingerprints.
Suite/probe JSON is retained unchanged as `.receipt.txt`; summary and provenance
are derived metadata, not standalone performance evidence. The parent's
`provenance.txt` maps the external originals and verifies copied bytes.
