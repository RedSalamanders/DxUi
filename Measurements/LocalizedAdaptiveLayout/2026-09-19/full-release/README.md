# Localized layout — complete x64 Release suite run

All 18 requested suites returned zero. Menu retains nine explicit interactive-desktop capability
skips; the other 17 suites record zero skips. This includes the constrained French scene, native
and embedded disclosure accessibility, multiline paint and measured action geometry. NativeTextInput
passed with zero recorded skips. This does not establish consumer mixed-DPI or screen-reader acceptance.

Command: `test.ps1 -Configuration Release -Platform x64`. Full build/runner log:
`C:/RedSalamander.Perf/evidence/i26-ui/dxui-full-release.log`.
The suite receipts bind executable hashes and the common benchmark; all suite logs are retained.
The common performance receipt is unpaired here. The adjacent isolated-process investigation
retains matched baseline/candidate comparisons, including every flagged comparison.

The first constrained-scene run incorrectly asserted no animation discovery was pending immediately
after replacing the root. The final fixture services the documented initial discovery tick before
checking that a hidden graph requests no further ticks. No production behavior or idle assertion
was weakened. Its first failure remains in the external `dxui-localized-release.log`.
