# Complete French Checkbox captions

The independent WARP pixel regression subtracts an empty-caption frame, excludes the indicator and single-line text band, and requires ink in additional lines of the long French sentence. At base 54e956e it fails with multiline enabled; the one-line painter correction honors the existing inherited option and keeps the default unchanged. The first callback test incorrectly called a disabled control directly; the final test uses host dispatch and preparation, the production input boundary. That rejected iteration is retained. Final targeted Release Embedded/Control/Accessibility passes, including acknowledgement without callback and Space/disabled behavior.

The first common performance candidate triggered clean FPS/p95/compose-CPU investigation bands and stopped its runner before correctness tests. Five further baseline/candidate pairs alternate execution order, with five rounds per measurement on the same native x64 Release WARP fixture, unchanged benchmark hash, toolchain and power policy. Original and individual noisy results remain retained. Median paired changes: clean FPS -1.01%, p95 +2.65%, compose CPU -1.80%, private bytes +0.79%; dirty FPS -0.37%, p95 -1.34%, compose CPU +1.65%, private bytes -1.18%. Allocations and surface sizes are unchanged. Directions reverse between repetitions, so the original flag is not confirmed as systematic degradation. This is an investigation result, not equivalence or a replacement baseline. Full configuration and gallery receipts follow separately.
