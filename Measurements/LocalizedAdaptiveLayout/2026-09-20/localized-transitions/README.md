# Localized transitions, selection and clipping

The final Release Embedded, Control and Accessibility suites pass with zero skips. The new tests use only library-owned models and the supplied WARP device. Manifest hashes identify the exact uncommitted test inputs against base 1ea876fc; production/include/build inputs remain identical to qualified 26a3d044. The initial test compile error (D2D point passed instead of the public PointDip type) and all later successful iterations are retained.

`TestLocalizedInvalidationAndSelectedDetail` exercises unchanged cache reuse, changed French button text, font/validation detail, stable selected ID 507 in 1000 grid rows, bounded row/cell work and matching row hit rectangles through 96/144/192/96 layout. It checks high contrast/reduced motion, Tab/Shift+Tab, Space/Enter and unowned Escape forwarding, release outside, capture cancellation after bounds change, hidden-focus pruning and root replacement. Clean composition has zero C++ allocations/preparation; hidden state releases its surface and performs no visual work.

`TestLocalizedStackedBodyClipping` composes two body panels and a separate footer. Pointer and real raw UIA Invoke hit only the footer through overlapping offscreen action geometry. Scrolling exposes the complete action rectangle and invokes only that action; retained providers disconnect after detach. The original action-layout and short-viewport fixtures are unchanged.

These are logical DPI/shared-library checks, not physical monitor or consumer acceptance. The runner's performance samples are unpaired. The retained [alternating baseline/candidate investigation](../../2026-09-19/interleaved-final/README.md) remains the production performance evidence, with its original flags and uncertainty; this test-only addition does not rebaseline it. The native full matrix is required on the published final revision before handoff. No public API or gallery pixels change in this qualification addition.
